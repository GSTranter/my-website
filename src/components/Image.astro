---
import { urlForImage } from "../sanity/lib/url-for-image";

const { asset, alt } = Astro.props.node;

const url = urlForImage(asset).width(430).url();
---

<picture>
    <img class="responsive__img" src={url} alt={alt} />
</picture>
